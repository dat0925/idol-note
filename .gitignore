node_modules/
.env
.env.*

# Claude Code の端末ごとのローカル設定（.mcp.json はコミットする）
.claude/settings.local.json
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# Supabase CLI のローカル設定（service_role キーが入りうる）
.supabase/
supabase/.branches/
supabase/.temp/

# 背景イラストの元データ（1枚2〜3MB）。
# publicリポジトリなのでコミットすると GitHub Pages から誰でも落とせてしまうし、
# 配信に使うのは tools/build-hero.py が書き出した assets/*.webp のほう。
# ★綴りゆれ（idle / idole / idol）をまとめて弾く。
#   idoleimage-p*.png を一度うっかりコミットしたことがある。
idleimage*.png
idoleimage*.png
idolimage*.png
hero-src*.png

# ロゴの元データ（2MB）。配信に使うのは tools/build-icons.py が書き出したほう
logo.png
